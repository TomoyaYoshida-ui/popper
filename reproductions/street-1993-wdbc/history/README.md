# Registration history

`registration-v1/` 保存引入论文 claim 与协议语义契约之前的 Popper 注册状态和运行日志。它绑定旧版 `paper.json`、`protocol.json`、执行器、核验器及产物哈希，仅用于迁移审计，不能作为当前任务状态继续执行。

当前有效状态位于任务根目录的 `.popper-reproduction/`，由新契约重新注册、执行并核验。
